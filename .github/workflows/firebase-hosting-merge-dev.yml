# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: Get yarn cache directory path
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - uses: actions/cache@v2
        id: yarn-cache
        with:
          path: |
            ${{ steps.yarn-cache-dir-path.outputs.dir }}
            ./node_modules/
          key: ${{ runner.os }}-yarn-${{ hashFiles('./yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - name: Install Yarn
        run: npm install -g yarn

      - name: Install Dependencies
        if: steps.yarn-cache.outputs.cache-hit != 'true'
        run: yarn --ignore-platform --ignore-engines --silent

      - name: Build
        run: yarn build
        env:
          CI: false
          NODE_OPTIONS: "--max-old-space-size=6144"
          VITE_BASE_API_URL: https://stg-api.distilled.ai/distill
          VITE_SOCKET_URL: wss://stg-api.distilled.ai
          VITE_GROUP_DEFAULT_FOR_PRIVATE_AGENT: 5
          VITE_AGENTLAND_BACKEND_URL: https://backend-release.agents.land
          VITE_MEME_BACKEND_URL: https://backend-meme.agents.land
          VITE_STAKING_AIRDROP_BACKEND_URL: https://airdrop-be-stag.agents.land
          VITE_SOLANA_RPC: https://devnet.helius-rpc.com/?api-key=3b28a0fc-0ef6-48ef-b55c-c55ae74cb6a6
          VITE_SOLANA_WS: wss://devnet.helius-rpc.com/?api-key=3b28a0fc-0ef6-48ef-b55c-c55ae74cb6a6
          VITE_GIT_COMMIT_ID: ${{ github.sha }}

      - name: Import Firebase service account credentials
        id: import-credentials
        run: |
          echo ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MESS_DISTILLED_AI }} | base64 -d > $(pwd)/serviceaccount.json
          echo "file_path=$(pwd)/serviceaccount.json" >> $GITHUB_OUTPUT

      - name: Deploy to Firebase
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ steps.import-credentials.outputs.file_path }}
        run: yarn firebase deploy --only hosting:mesh-distilled-ai-dev

      - name: Remove credentials file
        if: success() || failure()
        run: rm ${{ steps.import-credentials.outputs.file_path }}
